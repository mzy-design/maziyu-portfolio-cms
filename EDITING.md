# 内容修改指南

每一页的文字和图片路径分别放在 `content/`：

- `home.js`：首页
- `works.js`：作品列表
- `about.js`：关于
- `contact.js`：联系
- `project.js`：作品详情示例

把新图片放进 `images/`，再修改相应内容文件里的图片路径即可。新增作品详情时，复制 `work/` 文件夹并为它指定一个新的内容文件。纯静态 GitHub Pages 没有安全的在线上传后台；需要在线发布型 CMS 时，应接入带登录和对象存储的服务。
